# macOS
.DS_Store

# editor / swap cruft
*.swp
*~

# local dev state (if projects ever live alongside)
.direnv/
.devenv/

# agent scratch space (.claude/CLAUDE.md itself is tracked)
.claude/worktrees/
